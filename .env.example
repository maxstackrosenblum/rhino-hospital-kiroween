# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=appdb
POSTGRES_PORT=5433

# Backend Configuration
DATABASE_URL=postgresql://postgres:postgres@db:5432/appdb
SECRET_KEY=your-secret-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Frontend Configuration
# Use http://backend:8000 when running in Docker, http://localhost:8000 for local development
VITE_API_URL=http://localhost:8000

# Email Configuration (Optional - for password reset emails)
# Leave empty to skip email sending (tokens will be logged instead)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@hospital.com
SMTP_FROM_NAME=Hospital Management System
FRONTEND_URL=http://localhost:3000
